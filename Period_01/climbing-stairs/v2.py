from typing import List

"""
Задача: Climbing Stairs (Подъём по лестнице)
Сложность: Easy

Известное решение: 
Это классическая задача на числа Фибоначчи.
Количество способов подняться на n ступенек = F(n+1), где F - числа Фибоначчи:
F(0) = 0, F(1) = 1, F(2) = 1, F(3) = 2, F(4) = 3, F(5) = 5, F(6) = 8 и т.д.

Формула Бине для O(1) решения:
ways = round((phi^(n+1) - psi^(n+1)) / sqrt(5))
где phi = (1 + sqrt(5)) / 2 (золотое сечение), psi = (1 - sqrt(5)) / 2

Для n=45 это даёт точный результат в пределах целых чисел.
"""

class Solution:
    def climbStairs(self, n: int) -> int:
        """
        Подсчёт количества способов подняться на n ступенек,
        используя шаги длиной 1 или 2.
        
        Args:
            n: количество ступенек (1 <= n <= 45)
            
        Returns:
            количество различных способов подъёма
        """
        # Реализация через числа Фибоначчи с итеративным подходом O(n)
        # Для O(1) можно использовать формулу Бине
        
        # TODO: реализовать решение
        pass


# Тесты
def run_tests():
    solution = Solution()
    
    # Тест 1: n = 1
    assert solution.climbStairs(1) == 1, "Тест 1 провален: n=1"
    
    # Тест 2: n = 2
    assert solution.climbStairs(2) == 2, "Тест 2 провален: n=2"
    
    # Тест 3: n = 3
    assert solution.climbStairs(3) == 3, "Тест 3 провален: n=3"
    
    # Тест 4: n = 4
    assert solution.climbStairs(4) == 5, "Тест 4 провален: n=4"
    
    # Тест 5: n = 5
    assert solution.climbStairs(5) == 8, "Тест 5 провален: n=5"
    
    # Тест 6: n = 45 (максимальное значение по условию)
    assert solution.climbStairs(45) == 1836311903, "Тест 6 провален: n=45"
    
    print("Все тесты пройдены!")


# Запуск тестов
if __name__ == "__main__":
    run_tests()